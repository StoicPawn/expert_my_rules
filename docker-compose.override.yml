services:
  expert-my-rules:
    # Keep the Control Center document stable while setup runs. V4 polls the
    # lightweight JSON state endpoint instead of reloading the whole page.
    command: ["uvicorn", "awb.web.control_v4:control_app", "--host", "0.0.0.0", "--port", "8000"]
